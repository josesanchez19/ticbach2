def segundo_grado():
    import math
    x=input("dime el valor de a")
    y=input("dime el valor de b")
    z=input("dime el valor de c")
    h=0
    h=math.sqrt(y*y-4*x*z)
    j=0
    q=0
    j=(-y+h)/2*x
    q=(-y-h)/ 2*x
    if h==o:
        print "la salucion es unica y su valor es ",q
    if h<0:
        print "no tiene solucion"
    if h>0:
        print("la ecuacion tiene dos solucionesy son ",j,q       
segundo_grado()
